# Orchestrator-only environment.
# Copy to `neurons/orchestrator/.env` or set from your process manager.

CORE_SERVER_URL=https://beamcore.b1m.ai
ORCH_GATEWAY_URL=https://orch-gateway.b1m.ai
SUBTENSOR_NETWORK=finney
NETUID=105

# Worker pool topology (default: public = Option 2)
# WORKER_GATEWAY_MODE=public

# Option 1 — dedicated worker-gateway (requires mode=dedicated + all vars below)
# WORKER_GATEWAY_MODE=dedicated
# WORKER_GATEWAY_PUBLIC_URL=https://your-worker-gateway.example.com
# WORKER_GATEWAY_CONTROL_URL=ws://localhost:8001/control
# WORKER_GATEWAY_CONTROL_SECRET=your-long-random-secret
# READY=false

WALLET_NAME=orchestrator
WALLET_HOTKEY=default
WALLET_PATH=~/.bittensor/wallets

LOG_LEVEL=INFO

# Optional Slack incoming webhook for worker connect and task lifecycle alerts.
# SLACK_NOTIFICATION_URL=https://hooks.slack.com/services/...
