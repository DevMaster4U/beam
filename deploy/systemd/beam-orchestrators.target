[Unit]
Description=BEAM Orchestrators (all configured instances)
# Wants= lines are generated by install-systemd.sh / beam_sync_orchestrators from config/orchestrators/*.env

[Install]
WantedBy=multi-user.target
