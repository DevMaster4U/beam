[Unit]
Description=BEAM Worker Gateways (all configured instances)
# Wants= lines are generated by install-systemd.sh / beam_sync_gateways from config/gateways/*.env

[Install]
WantedBy=multi-user.target
